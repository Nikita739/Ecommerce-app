import React, {useCallback, useContext, useState} from 'react';
import cl from './Auth.module.css'

const Auth = () => {
    return (
        <div className={cl.outer}>
            <h1>Reporting illegal action. Please wait...</h1>
        </div>
    );
};

export default Auth;